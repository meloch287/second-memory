# Plan — «Толик» bot overhaul (branch Tolik)

Goal: fix reported bugs + turn the bot from a notebook into a personal assistant with a
settings «личный кабинет» (stats, Фитнес stub, Долги CRUD, Вишлист with gallery). All reminders
correct in MSK. Everything persists.

Verification: `node --test` (deterministic) + spy-bot handler drives for flows. Holdout checks
named `*.holdout.test.mjs`.

## Units (ordered; heavy file overlap on telegram*.mjs/store.mjs → run mostly SEQUENTIAL,
## checkpoint-commit each green unit on branch Tolik)

### U0 — Identity: bot is always «Толик»
- default botName 'Толик'; onboarding drops "name the bot", greets as Толик, asks user's name → rhythm → tz.
- friendSystem persona says Толик. Old default "Вторая память"/empty botName → Толик at read time.
- AC: fresh /start greets as Толик & asks user name first; getUser botName defaults Толик; regression: existing flows pass.

### U1 — Remove summary/«Итог»/«саммари» from the bot
- parser: drop summary intent; brain: drop summary case; telegram menu + /help: remove Итог.
- AC: «итог»/«саммари» no longer produce a summary via the bot; menu has no Итог; suite green.

### U2 — Reminder correctness (the reported bug) [CRITICAL]
- future-date reminder ("напомни 29 июля …") → task due that date (noon MSK if no time), fires ON it.
- interval reminder ("через 10 мин / 3 дня / 2 часа") → fires exactly then, MSK.
- "напомни … должно прийти 95к по зп / пришло по зп / переспроси 29-го" → reminder task, NOT debt.
- AC (deterministic + spy-bot scheduler): correct type/due/MSK; not debt; scheduler fires once at right minute; no dup.

### U3a — /settings личный кабинет
- /settings + «настройки» → inline LK: stats (requests counter [new], facts, open debts, open tasks, days) + buttons Фитнес/Долги/Вишлист. Фитнес = stub.
- AC: LK renders stats + 3 buttons; Фитнес callback → "в разработке".

### U3b — Долги CRUD (inline + voice)
- «Долги» → list (кто/сумма/направление/срок/статус) + add/edit/delete buttons; voice/text add persists; correct reminders.
- AC: list renders; callback add/del/edit persist; voice-added debt persists & reflected.

### U3c — Вишлист (list + gallery + add URL/manual) [RESIDUAL-RISK on URL photo parse]
- store `wishlist`; «Вишлист» list; «+» → по ссылке(авто)/вручную; «Посмотреть фото» gallery
  "N/M title + photos + desc + link" with ‹ | › nav + Назад; middle | no-op.
- URL auto-parse best-effort (antibot); manual guaranteed.
- AC: manual add + list + gallery nav work & persist; URL add saves item (photos best-effort).

### U4 — Persistence after every CRUD
- every add/edit/delete → store persists (debounced flush ok) & reflected. AC: reopen store → data present.

## Out of scope
- Фитнес implementation (next phase, per user). Web/browser version (frozen). New repo (use existing).
