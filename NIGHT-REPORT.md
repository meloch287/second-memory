# NIGHT-REPORT — Толик (ветка `Tolik`)

**Вердикт: done** (с 2 намеренно отложенными мелочами — см. Residual). Все заявленные баги и фичи сделаны, проверены и закоммичены. `node --test` → **254 / 254 pass / 0 fail** (было 162). Ветка `Tolik` в `meloch287/second-memory` (приватный, существующий репо — новый не заводил, см. `docs/ASSUMPTIONS.md`).

Как запустить: `node src/server.mjs` (бот в том же процессе; токен `TELEGRAM_BOT_TOKEN` в `.env`). Тесты: `node --test`.

## Shipped (с доказательствами)

| # | Что | Юнит-коммит | Доказательство |
|---|-----|-------------|----------------|
| 0 | Бот теперь всегда **Толик** — при /start представляется Толиком и спрашивает ИМЯ пользователя (шаг «назови бота» убран); старый/пустой botName → Толик | `6068c03` | тесты онбординга + `friendSystem` дефолт; e2e шаг 1 PASS |
| 1 | Команда **саммари/«Итог» убрана** из бота (parser/brain/меню/help) | `6068c03` | `parseMessage('итог'|'саммари').kind !== 'summary'`; e2e шаг 6 PASS |
| 2 | **Корректные напоминания (главный баг).** «напомни …» всегда задача, не долг; «должно прийти/пришло 95к по зп» — не долг; будущая дата (29 июля) и интервалы (через 10 мин / 3 дня / 2 часа) срабатывают ровно вовремя по МСК | `5b0e66e` | `test/reminder-correctness.test.mjs` — e2e-планировщик спай-ботом: не срабатывает раньше, срабатывает 29 июля один раз, без дублей; e2e шаги 5a/5b PASS. **Корень:** долг-триггер стоял раньше reminder-ветки, «должно» ловилось как долг |
| 3a | **Личный кабинет** `/settings` + «настройки»/«лк»: статы (запросов Толику, фактов, долгов, задач, встреч, вишлист, дней) + кнопки **[Фитнес]**(заглушка «в разработке») **[Долги] [Вишлист]** | `c5f8ea6`,`e6c7785` | `test/lk-debts.test.mjs`; e2e шаг 2 PASS |
| 3b | **Долги** — список (кто/сумма/направление/срок), inline-кнопки добавить/редактировать/погасить/удалить; добавление текстом ИЛИ голосом; напоминания корректны | `e6c7785` | `test/lk-debts.test.mjs`; e2e шаг 3 PASS |
| 3c | **Вишлист** — список; добавление **по ссылке** (автопарс названия/описания/фото/цены через `wlparse`) **или вручную**; **галерея** «N/M — товар + фото + описание + ссылка» с кнопками `‹ | ›` (средняя — пустышка) и «Назад»; редактирование/удаление | `7a8f1c5`,`cfa0e09` | `test/wlparse.test.mjs` (11), `test/lk-wishlist.test.mjs` (17); e2e шаг 4 PASS |
| 4 | **Толик всё помнит** — любое добавление/редактирование/удаление персистится в store (счётчик запросов, вишлист-CRUD, migrate/wipe учитывают вишлист) | `c5f8ea6`,`c29ebcc` | `test/wishlist.test.mjs`, `test/store.test.mjs` (reopen → данные на месте) |
| — | **Без гс тоже работает** (доп-требование): единый маршрут текст+голос через ЛК — и текст, и транскрипт голоса проходят `LK_TRIGGER_RE`/`consumeInput` | `e035185` | `test/voice-parity.test.mjs` |
| — | Ревью-фиксы: **двойное сохранение записи** при выкл. ИИ; гейт онбординга для ЛК; отмена/валидация в add-by-URL; мёртвая ветка deep-link | `e035185` | `test/telegram-nodup.e2e.test.mjs` + 5/5 регрессов red→green |

Рефакторинг: все исходники ≤700 строк (макс `parser.mjs` 685). Новые модули: `src/telegram-lk.mjs` (ЛК/долги/вишлист), `src/wlparse.mjs` (парсер товара).

Гейт: адверсариальное ревью (бот поднят вживую, проводка исправна) + held-out e2e (`test/tolik.e2e.holdout.test.mjs`, гоняет реальный `startTelegramBot` спай-транспортом по всему пути) — оба зелёные.

## Residual (отложено намеренно)
1. **Словесные даты** («двадцать девятого июля») парсер не понимает — числовые («29 июля») работают. Расширять `dates.mjs` — отдельная задача.
2. **Автопарс фото с Ozon/Я.Маркет/WB** с datacenter-IP часто блокируется antibot'ом → подтянется максимум слаг/название; при отсутствии фото Толик предлагает ручной ввод. Полные фото реально тянуть с VPS (103.88.241.202) — вынесено на потом. Ручное добавление работает всегда.
3. Мелкое: счётчик «Запросов Толику» считает и команды; голосовые в счётчик не попадают (некритично).

## Не в этой фазе (по твоим словам)
- Реализация **Фитнеса** (личный тренер) — заглушка стоит, логику скажешь позже.
- Веб/браузерная версия — заморожена, не трогалась.

## Assumptions
См. `docs/ASSUMPTIONS.md` (репо=существующий, ветка Tolik; Толик — постоянное имя; напоминания в МСК; долги/вишлист/настройки работают без гс).

Репо: `git@github.com:meloch287/second-memory.git`, ветка `Tolik` (запушена).
